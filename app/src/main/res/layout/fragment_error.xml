<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@mipmap/ic_background_light"
    tools:context=".menu.explore.Fragment_Error">

    <ImageView
        android:id="@+id/iv_error_cat"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:layout_above="@+id/tv_error_hint"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"
        android:src="@drawable/ic_error_1" />

    <TextView
        android:id="@+id/tv_error_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="@string/error_fragment_code"
        android:textColor="@color/white_transparent_80"
        android:textSize="21sp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tv_error_hint"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        android:text="@string/error_fragment_hint"
        android:textColor="@color/white_transparent_60"
        android:textSize="17sp" />

</RelativeLayout>